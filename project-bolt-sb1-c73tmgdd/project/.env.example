# Frontend Environment Variables (Vite)
# These are baked into the build at compile time. Safe to commit — no secrets here.

# URL of the AgentVault dashboard API server
# For local dev: http://localhost:3099
# For production: https://your-vps-ip:3099 or behind a reverse proxy
VITE_API_URL=http://localhost:3099
